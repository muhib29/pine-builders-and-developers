# Sanity CMS Configuration
# Get these values from your Sanity project dashboard at https://www.sanity.io/manage
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01

# Public site URL (used for Open Graph absolute URLs)
NEXT_PUBLIC_SITE_URL=localhost:3000

# SendGrid (used by server API to send contact form emails)
# Get a SendGrid API key and set it in your deployment environment.
SENDGRID_API_KEY=your_sendgrid_api_key_here
# The email address SendGrid will use as the sender (must be verified in SendGrid)
SENDER_EMAIL=no-reply@your-domain.com

# SMTP fallback (if you can't use SendGrid)
# Example for Gmail SMTP (App password required if using Google accounts):
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password_or_app_password
# Optional: override from/to addresses
SMTP_FROM=verified-sender@your-domain.com
TO_EMAIL=pinebuilder83@gmail.com
